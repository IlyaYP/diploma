package logging

const (
	// CorrelationIDKey defines the logging key for tracking the Correlation ID.
	CorrelationIDKey = "correlation_id"

	// ServiceKey defines the logging key for tracking the service.
	ServiceKey = "service"

	// OrderIDKey defines the logging key for tracking the Order ID.
	OrderIDKey = "order_id"

	// BarcodeKey defines the logging key for tracking the Barcode.
	BarcodeKey = "barcode"

	// UserLoginKey defines the logging key for tracking the User.
	UserLoginKey = "login"

	// IDKey defines logging key to track object ID.
	IDKey = "id"
)
